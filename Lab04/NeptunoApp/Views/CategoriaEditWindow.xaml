<Window x:Class="NeptunoApp.Views.CategoriaEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TituloVentana}" Height="320" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding TituloVentana}" Style="{StaticResource TituloSeccion}"
                   Margin="0,0,0,18"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Nombre" Style="{StaticResource EtiquetaCampo}"/>
            <TextBox x:Name="NombreBox" Style="{StaticResource CampoTexto}" MaxLength="30"
                     Text="{Binding NombreCategoria, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,14,0,0">
            <TextBlock Text="Descripción" Style="{StaticResource EtiquetaCampo}"/>
            <TextBox Style="{StaticResource CampoTexto}" MaxLength="200" Height="80"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"
                     Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancelar" Style="{StaticResource BotonSecundario}"
                    Click="Cancelar_Click" IsCancel="True" Margin="0,0,8,0"/>
            <Button Content="Guardar" Style="{StaticResource BotonPrimario}"
                    Click="Guardar_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
