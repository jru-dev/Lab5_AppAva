<Window x:Class="NeptunoApp.Views.ProductoEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TituloVentana}" Height="530" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding TituloVentana}" Style="{StaticResource TituloSeccion}"
                   Margin="0,0,0,18"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,12">
                <TextBlock Text="Nombre del producto" Style="{StaticResource EtiquetaCampo}"/>
                <TextBox x:Name="NombreBox" Style="{StaticResource CampoTexto}" MaxLength="60"
                         Text="{Binding NombreProducto, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,12">
                <TextBlock Text="Categoría" Style="{StaticResource EtiquetaCampo}"/>
                <ComboBox ItemsSource="{Binding Categorias}"
                          SelectedItem="{Binding CategoriaSeleccionada}"
                          DisplayMemberPath="NombreCategoria"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,12">
                <TextBlock Text="Proveedor" Style="{StaticResource EtiquetaCampo}"/>
                <ComboBox ItemsSource="{Binding Proveedores}"
                          SelectedItem="{Binding ProveedorSeleccionado}"
                          DisplayMemberPath="CompaniaNombre"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,12">
                <TextBlock Text="Presentación" Style="{StaticResource EtiquetaCampo}"/>
                <TextBox Style="{StaticResource CampoTexto}" MaxLength="30"
                         Text="{Binding CantidadPorUnidad, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,12">
                <TextBlock Text="Precio unitario" Style="{StaticResource EtiquetaCampo}"/>
                <TextBox Style="{StaticResource CampoTexto}"
                         Text="{Binding PrecioUnidad, StringFormat='N2', UpdateSourceTrigger=LostFocus}"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,12">
                <TextBlock Text="Unidades en existencia" Style="{StaticResource EtiquetaCampo}"/>
                <TextBox Style="{StaticResource CampoTexto}"
                         Text="{Binding UnidadesEnExistencia, UpdateSourceTrigger=LostFocus}"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,12">
                <TextBlock Text="Unidades en pedido" Style="{StaticResource EtiquetaCampo}"/>
                <TextBox Style="{StaticResource CampoTexto}"
                         Text="{Binding UnidadesEnPedido, UpdateSourceTrigger=LostFocus}"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,12">
                <TextBlock Text="Nivel de reorden" Style="{StaticResource EtiquetaCampo}"/>
                <TextBox Style="{StaticResource CampoTexto}"
                         Text="{Binding NivelDeReorden, UpdateSourceTrigger=LostFocus}"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Grid.Column="2" Margin="0,0,0,12" VerticalAlignment="Bottom">
                <CheckBox Content="Producto descontinuado" Margin="0,0,0,8"
                          IsChecked="{Binding Descontinuado}"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" Style="{StaticResource BotonSecundario}"
                    Click="Cancelar_Click" IsCancel="True" Margin="0,0,8,0"/>
            <Button Content="Guardar" Style="{StaticResource BotonPrimario}"
                    Click="Guardar_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
