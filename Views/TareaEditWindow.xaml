<Window x:Class="WPF_SP.Views.TareaEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TituloVentana}" Height="330" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBlock Text="&#xE70F;" FontFamily="{StaticResource IconFont}" FontSize="18"
                           Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding TituloVentana}" FontSize="18" FontWeight="Bold"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Text="Título" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,20,0,6"/>
            <TextBox Grid.Row="2" Style="{StaticResource ModernTextBox}" Text="{Binding Titulo, UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="150" x:Name="TituloBox"/>

            <TextBlock Grid.Row="3" Text="Descripción" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,16,0,6"/>
            <TextBox Grid.Row="4" Style="{StaticResource ModernTextBox}" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="70" VerticalContentAlignment="Top"/>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancelar" Style="{StaticResource OutlineButton}" Width="100" Margin="0,0,10,0"
                        Click="Cancelar_Click"/>
                <Button Content="Guardar" Style="{StaticResource AccentButton}" Width="110"
                        IsDefault="True" Click="Guardar_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
